---
---

<section class="portal-section" style="background:var(--background);padding:80px 0;">
  <div style="max-width:1000px;margin:0 auto;padding:0 4vw;">
    <div style="text-align:center;margin-bottom:60px;">
      <h2 style="color:var(--accent-light);font-size:2.5rem;margin-bottom:20px;font-weight:600;">
        🔐 <span style="color:#fff;">Portal Cliente Seguro</span>
      </h2>
      <p style="color:var(--subtext);font-size:1.2rem;line-height:1.6;max-width:600px;margin:0 auto;">
        Accede a tu caso en tiempo real con máxima seguridad y transparencia total
      </p>
    </div>
    
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:60px;align-items:center;">
      <!-- Portal Preview -->
      <div class="portal-preview">
        <div class="portal-window">
          <div class="portal-header">
            <div class="portal-dots">
              <span style="background:#ff5f57;"></span>
              <span style="background:#ffbd2e;"></span>
              <span style="background:#28ca42;"></span>
            </div>
            <div style="color:var(--subtext);font-size:0.9rem;">🔒 Portal Seguro PDC</div>
          </div>
          <div class="portal-content">
            <div class="portal-nav">
              <div class="nav-item active">📊 Dashboard</div>
              <div class="nav-item">📋 Mi Caso</div>
              <div class="nav-item">💬 Mensajes</div>
              <div class="nav-item">📁 Evidencia</div>
            </div>
            <div class="portal-main">
              <div class="case-status">
                <h4>🎯 Estado del Caso: <span style="color:#28ca42;">EN PROGRESO</span></h4>
                <div class="progress-bar">
                  <div class="progress-fill" style="width:65%;"></div>
                </div>
                <p style="font-size:0.9rem;color:var(--subtext);">65% Completado - Estimado: 2 días</p>
              </div>
              <div class="recent-updates">
                <h5>📢 Actualizaciones Recientes</h5>
                <div class="update-item">
                  <span class="update-time">Hace 2h</span>
                  <span class="update-text">Nueva evidencia recolectada</span>
                </div>
                <div class="update-item">
                  <span class="update-time">Hace 5h</span>
                  <span class="update-text">Análisis OSINT completado</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Features -->
      <div class="portal-features">
        <div class="feature-item">
          <div class="feature-icon">📊</div>
          <div>
            <h4>Dashboard en Tiempo Real</h4>
            <p>Seguimiento completo del progreso de tu caso con actualizaciones automáticas cada hora.</p>
          </div>
        </div>
        
        <div class="feature-item">
          <div class="feature-icon">🔐</div>
          <div>
            <h4>Comunicación Encriptada</h4>
            <p>Chat seguro con tu investigador asignado. Todas las conversaciones protegidas end-to-end.</p>
          </div>
        </div>
        
        <div class="feature-item">
          <div class="feature-icon">📁</div>
          <div>
            <h4>Evidencia Digital Segura</h4>
            <p>Acceso inmediato a toda la evidencia recolectada con timestamps y validación legal.</p>
          </div>
        </div>
        
        <div class="feature-item">
          <div class="feature-icon">📱</div>
          <div>
            <h4>Acceso Móvil 24/7</h4>
            <p>Portal optimizado para móviles. Mantente informado desde cualquier lugar del mundo.</p>
          </div>
        </div>
        
        <div style="text-align:center;margin-top:40px;">
          <a href="#" class="portal-cta" onclick="alert('Portal disponible una vez iniciado el caso')">
            🚀 ACCEDER AL PORTAL
          </a>
          <p style="font-size:0.9rem;color:var(--accent);margin-top:10px;font-style:italic;">
            *Disponible automáticamente al contratar cualquier servicio
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
.portal-preview {
  position: relative;
}

.portal-window {
  background: var(--surface);
  border-radius: 15px;
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(0,0,0,0.4);
  border: 1px solid var(--border);
}

.portal-header {
  background: var(--card);
  padding: 15px 20px;
  display: flex;
  align-items: center;
  gap: 15px;
  border-bottom: 1px solid var(--border);
}

.portal-dots {
  display: flex;
  gap: 8px;
}

.portal-dots span {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  display: block;
}

.portal-content {
  display: flex;
  min-height: 300px;
}

.portal-nav {
  background: var(--surface-alt);
  width: 120px;
  padding: 20px 0;
  border-right: 1px solid var(--border);
}

.nav-item {
  padding: 12px 20px;
  font-size: 0.85rem;
  color: var(--subtext);
  cursor: pointer;
  transition: all 0.3s ease;
}

.nav-item.active {
  background: var(--accent);
  color: var(--background);
  font-weight: 600;
}

.nav-item:hover:not(.active) {
  background: rgba(255,255,255,0.05);
  color: var(--text);
}

.portal-main {
  flex: 1;
  padding: 25px;
}

.case-status h4 {
  color: var(--text);
  margin-bottom: 15px;
  font-size: 1.1rem;
}

.progress-bar {
  background: var(--border);
  height: 8px;
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 10px;
}

.progress-fill {
  background: linear-gradient(90deg, var(--accent) 0%, #28ca42 100%);
  height: 100%;
  border-radius: 4px;
  transition: width 0.3s ease;
}

.recent-updates {
  margin-top: 25px;
}

.recent-updates h5 {
  color: var(--accent-light);
  margin-bottom: 15px;
  font-size: 1rem;
}

.update-item {
  display: flex;
  justify-content: space-between;
  padding: 8px 0;
  border-bottom: 1px solid rgba(255,255,255,0.1);
  font-size: 0.9rem;
}

.update-time {
  color: var(--accent);
  font-weight: 500;
}

.update-text {
  color: var(--subtext);
}

.portal-features {
  display: flex;
  flex-direction: column;
  gap: 30px;
}

.feature-item {
  display: flex;
  gap: 20px;
  align-items: flex-start;
}

.feature-icon {
  font-size: 2.5rem;
  filter: drop-shadow(0 0 8px rgba(255,255,255,0.3));
  flex-shrink: 0;
}

.feature-item h4 {
  color: var(--text);
  margin-bottom: 8px;
  font-size: 1.2rem;
  font-weight: 600;
}

.feature-item p {
  color: var(--subtext);
  line-height: 1.6;
  margin: 0;
}

.portal-cta {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  background: linear-gradient(135deg, var(--accent) 0%, var(--accent-light) 100%);
  color: var(--background);
  padding: 15px 30px;
  border-radius: 25px;
  text-decoration: none;
  font-weight: 600;
  font-size: 1.1rem;
  transition: all 0.3s ease;
  box-shadow: 0 10px 30px rgba(0,0,0,0.3);
}

.portal-cta:hover {
  transform: translateY(-3px);
  box-shadow: 0 15px 40px rgba(0,0,0,0.4);
  background: linear-gradient(135deg, var(--accent-light) 0%, #ffffff 100%);
}

@media (max-width: 768px) {
  .portal-section > div > div {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  
  .portal-content {
    flex-direction: column;
  }
  
  .portal-nav {
    width: 100%;
    display: flex;
    overflow-x: auto;
    padding: 15px 0;
  }
  
  .nav-item {
    white-space: nowrap;
    padding: 10px 15px;
  }
  
  .feature-item {
    gap: 15px;
  }
  
  .feature-icon {
    font-size: 2rem;
  }
}
</style>